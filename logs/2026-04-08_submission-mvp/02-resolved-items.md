# Resolved Items

- The coursework entrypoint now runs from `submission/app.py`.
- Business logic stays outside the submission UI and lives in `packages/services/`.
- Prompt text and tone policy live under `packages/prompts/`.
- The MVP ships with deterministic stage stubs so the full pipeline can run without a provider key.
