# Plan

1. Align the architecture document with the agreed pipeline contracts.
2. Add typed domain models in `packages/core/models/`.
3. Implement separable stage services plus a thin orchestration pipeline.
4. Connect the submission UI to the real pipeline through bootstrap wiring.
5. Add contract and smoke tests to validate the initial slice.
