# Status Board

## 현재 진행 중
- sentiment traceability hardening for the submission pipeline
- deterministic audit and JSONL runtime logging for selected sentiment runs

## 방금 끝남
- fallback generation rewritten so each supported sentiment keeps a distinct grounded tone
- fallback evaluation now scores sentiment fit from detected cues instead of a flat placeholder

## 이제 안 봐도 됨
- narrow-screen result visibility fix from the earlier submission UI pass
- previous busy-state click regression on `Generate Story`
