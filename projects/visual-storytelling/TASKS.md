# TASKS.md

## Current Focus
- keep repository rules aligned with the mandatory inspect -> design -> attempt -> evaluate -> revise -> record loop
- keep project, skill, and design documents aligned so future implementation work starts from one consistent document stack
- preserve the thin `submission/app.py` boundary while keeping business logic in `packages/services` and `packages/core`

## Current Loop State
### Last completed step
- aligned the project-level execution docs and `DESIGN.md` with the visual storytelling MVP architecture

### Last observed failure type
- `SCHEMA_DRIFT_ERROR`

### Current hypothesis
- tighter alignment across `PROJECT.md`, skill docs, architecture docs, and `DESIGN.md` will reduce drift between product intent, UI guidance, and future implementation changes

### Next smallest useful task
- compare the current implementation, especially `submission/app.py` and trace presentation, against the updated document stack and close the largest UI/pipeline gaps first

## Change Log
### 2026-04-10
- changed files: `README.md`, `streamlit_app.py`, `apps/web/streamlit_app.py`, `apps/web/streamlit_presenter.py`, `apps/web/streamlit_styles.py`, `apps/web/README.md`, `packages/infra/upload_persistence.py`, `.streamlit/config.toml`, `requirements.txt`, `pyproject.toml`, `assets/raw_samples/*`, `assets/screenshots/*`, `assets/example_runs/*`, `assets/diagrams/system_architecture.svg`, `submission_package/README.md`, `submission_package/실습3_조수빈.ipynb`, `tests/apps/test_streamlit_presenter.py`, `tests/infra/test_upload_persistence.py`, `logs/runtime/2026-04-10_sentiment_runs.jsonl`, `projects/visual-storytelling/TASKS.md`
- what improved: Closed the submission-quality documentation gap and added a deployment-facing Streamlit surface without moving storytelling logic into the UI layer. The repo now has a product-style README with a visual architecture diagram, real screenshots, expected-output examples, deployment notes, repository map, and a notebook-centered submission package. The Streamlit app reuses the shared storytelling pipeline, includes built-in sample sequences, surfaces confidence-aware result states, and stays runnable even when `OPENAI_API_KEY` is missing by using the deterministic fallback path. Support assets were organized under `assets/` so the workflow is understandable from raw examples through final screenshots and saved run artifacts.
- evidence: Ran `python -m compileall apps/web packages/infra tests/apps tests/infra streamlit_app.py`, `pytest -q tests/apps/test_streamlit_presenter.py tests/infra/test_upload_persistence.py` with `7 passed`, and repeated Selenium-driven local Streamlit smoke checks for idle, generating, successful generation, low-confidence, and blocked states while capturing the screenshot set in `assets/screenshots/`. Also saved provider-backed and fallback example runs in `assets/example_runs/` and generated the final notebook artifact at `submission_package/실습3_조수빈.ipynb`.
- remaining issue: The repository is ready for a permanent Streamlit Community Cloud publish, but creating the final hosted `streamlit.app` URL still requires repository-owner authentication inside the Streamlit Cloud dashboard. The current README therefore includes a temporary live preview link from the active workspace session and explicitly documents the remaining manual publish step.
- next best step: Complete the Community Cloud publish from the repo owner account, then replace the temporary preview link in the README and notebook with the permanent hosted Streamlit URL.

### 2026-04-10
- changed files: `submission/styles.py`, `tests/submission/test_app.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Fixed the submission-layer `UI_STATE_ERROR` caused by aggressive Gradio CSS overrides. The uploader now keeps the native `.file-preview` rows readable by resetting those rows directly instead of forcing broad descendant colors, and action buttons are styled only through the app’s own button classes so Gradio internal upload/clear/tab buttons are no longer crushed by generic `button` rules. The nested `panel-card` styler wrappers are also forced transparent so the action area no longer sits inside heavy-looking internal slabs.
- evidence: Ran `python -m compileall submission tests/submission`, `pytest -q tests/submission/test_app.py tests/submission/test_presentation.py` with `29 passed`, and `pytest -q tests/e2e/test_browser_submission_flow.py` with `5 passed`. Real browser verification used exactly 3 uploaded images against a stubbed pipeline and confirmed file-row backgrounds of `rgb(242, 233, 220)`, `rgb(255, 253, 249)`, and `rgb(242, 233, 220)` with dark readable text, stable 48-50px action-button heights, intact layout, and a successful `Generate Story` run that rendered `Browser Verified Story`.
- remaining issue: The tiny upload-panel icon controls remain mostly native Gradio controls by design. They are intentionally left minimally styled so future CSS changes are less likely to break uploader internals again.
- next best step: Add one browser regression that uploads exactly 3 files and asserts the `.file-preview tr.file` rows stay on light backgrounds while the main action buttons keep their expected height range.

### 2026-04-10
- changed files: `submission/app.py`, `submission/presentation.py`, `submission/styles.py`, `tests/submission/test_app.py`, `tests/submission/test_presentation.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Completed a focused submission-layer UI polish pass for hierarchy and state clarity without changing the product structure. `Generate Story` now has a stronger primary treatment, follow-up actions are split from advanced corrected-analysis actions, disabled prerequisites are explained in a lighter action-hierarchy panel, the semantic palette now clearly separates neutral, in-progress, success, warning/low-confidence, and error states, the status banner is calmer and less visually heavy, and the results tabs have a much clearer active state with reduced nested-panel noise.
- evidence: Ran `python -m compileall submission tests/submission`, `pytest -q tests/submission/test_app.py tests/submission/test_presentation.py tests/submission/test_controller.py` with `40 passed`, and real browser verification with `pytest -q tests/e2e/test_browser_submission_flow.py` with `5 passed`. The browser run exercised the idle/blocked state, generating timer state, generated-result state, error/blocked state, and narrow stacked layout.
- remaining issue: The improved disabled guidance is still provided through the grouped action-availability panel rather than per-button native tooltips because the current Gradio button API does not expose a stable tooltip surface for these controls.
- next best step: If this polish pass needs one more refinement, add a lightweight visual browser regression that captures screenshots for idle, generating, success, and error states so future spacing/color tweaks can be reviewed against a fixed visual baseline.

### 2026-04-10
- changed files: `submission/styles.py`, `tests/submission/test_app.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Added controlled decorative color back into the sentiment selector without reintroducing dark component surfaces. Each sentiment pill now has its own soft color and selected border accent, and the native radio circle uses `accent-color` so the selected dot visibly fills with the matching sentiment color while text remains black.
- evidence: Added a CSS regression for the sentiment radio accent and selected-pill soft background rules. Ran `python -m compileall submission tests/submission && python -m pytest -q tests/submission/test_app.py tests/submission/test_presentation.py` with 28 passing tests. Browser-checked `http://127.0.0.1:7895` after clicking Sad: the radio is selected, `accent-color` is `rgb(59, 130, 246)`, the selected pill fill is `rgb(219, 234, 254)`, the selected outline is `rgb(59, 130, 246)`, and the upload surface remains light with black text.
- remaining issue: This is a visual-only enhancement; no generation logic, state handling, or layout behavior changed.
- next best step: Check the real browser selector once after refresh to confirm the selected pill color feels strong enough without hurting readability.

### 2026-04-10
- changed files: `submission/styles.py`, `tests/submission/test_app.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Fixed the remaining dark Gradio component surfaces. Browser dark mode could still leave Gradio-owned wrappers such as file upload drop zones, tabs, forms, blocks, and disabled buttons with dark backgrounds even though the custom story cards were light. The CSS now overrides those Gradio surface variables and component selectors directly so upload/drop areas, tab panels, button rows, and form controls stay white or very light gray with black text.
- evidence: Added a submission CSS regression that checks the explicit Gradio dark-surface overrides for file upload, block backgrounds, and disabled buttons. Verification will re-run the focused submission app/presentation tests before pushing.
- remaining issue: This is still a color-only fix; no generation logic, layout sizing, or event binding changed.
- next best step: Refresh the currently open browser tab or use the newly launched local URL after this patch, because older already-running Gradio servers can keep the previous CSS in memory.

### 2026-04-10
- changed files: `submission/styles.py`, `submission/presentation.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Refined the color-only UI pass for stronger readability. Inner cards, panels, guidance boxes, analysis cards, comparison blocks, table cells, and empty-state surfaces now use white or very light gray fills, while primary body and helper copy use near-black text. Accent colors remain only for action emphasis and status/category cues.
- evidence: Ran a color scan to confirm the previous sage-tinted surface literals were removed from the main UI color surfaces. Verification will re-run the submission presentation/app tests before pushing.
- remaining issue: This remains a palette-only change; no layout, event binding, generation flow, or responsive behavior was changed.
- next best step: Do a quick browser visual check at normal and narrow widths to confirm the brighter palette feels comfortable in the real UI.

### 2026-04-10
- changed files: `submission/styles.py`, `submission/presentation.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Updated only the submission UI color palette. The warm beige/brown palette was replaced with a lighter sage/teal surface system and coral primary action accent, while preserving all layout, button state, generation flow, responsive rules, and component structure.
- evidence: Verified the color-only change with `python -m compileall submission`, `pytest -q tests/submission/test_app.py tests/submission/test_presentation.py` (`27 passed`), and a color scan confirming the old beige/brown CSS literals were removed from the UI color surfaces.
- remaining issue: This pass intentionally did not change spacing, typography, layout, Streamlit wiring, README content, or notebook artifacts.
- next best step: Use the running local app to do a quick visual preference check, then make only palette-level adjustments if the hue should be warmer/cooler.

### 2026-04-09
- changed files: `submission/app.py`, `submission/controller.py`, `submission/presentation.py`, `submission/styles.py`, `tests/submission/test_controller.py`, `tests/submission/test_presentation.py`, `tests/e2e/test_browser_submission_flow.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Closed the remaining action-button `UI_STATE_ERROR`. The generation controls now have explicit prerequisite gating: `Generate Story` requires uploaded images plus valid input, regenerate actions require a previous run, and corrected-analysis actions require at least one saved Analysis correction. The controller validates prerequisites before entering the streaming worker path, so blocked actions show an immediate clear message instead of briefly displaying `Generating story...`. While real generation is running, all generation actions are disabled, the active button gets a busy label, the status banner shows only the processing state, and success or failure replaces that state instead of coexisting with stale blocked/error copy. Error banner copy also no longer double-escapes already-rendered HTML entities in the browser.
- evidence: Added controller/presentation coverage for control-state updates, disabled-state guidance, streamed processing output, and escaped error rendering. Added browser E2E coverage for initial disabled states, upload-enabled `Generate Story`, post-run regenerate unlock, correction-gated actions, processing timer behavior, failure-state replacement, and the existing narrow-layout regression. Verification passed with `python -m compileall submission tests/submission tests/e2e`, `pytest -q tests/submission/test_app.py tests/submission/test_controller.py tests/submission/test_presentation.py` (`38 passed`), and `pytest -q tests/e2e/test_browser_submission_flow.py` (`5 passed`).
- remaining issue: Disabled buttons are explained in the helper panel rather than native per-button tooltips because Gradio `Button` does not expose a stable tooltip API across versions.
- next best step: If the action area keeps growing, split the secondary actions into labeled groups such as `Run again` and `Use corrections` so the dependency model is even clearer visually.

### 2026-04-09
- changed files: `submission/app.py`, `submission/controller.py`, `submission/presentation.py`, `submission/styles.py`, `tests/submission/test_controller.py`, `tests/submission/test_presentation.py`, `tests/e2e/test_browser_submission_flow.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Added a clear in-progress generation UX to the submission app without reintroducing brittle browser-side runtime hooks. Generation actions now run through streaming controller wrappers that immediately push a visible `Generating story... / Processing... 0s` status banner, keep updating the elapsed seconds once per second while the pipeline runs in a worker thread, disable all generation buttons during the run, and restore button labels/interactivity when the run finishes. Failures now also exit that in-progress state cleanly because the wrapper restores the buttons and swaps in visible error output rather than leaving the UI ambiguous.
- evidence: Added controller/presentation regressions for the streamed processing state and browser e2e coverage for the real UI path: upload image, click `Generate Story`, observe `Generating story...` plus a `Processing... Ns` timer, verify the button is disabled during the run, then confirm it returns to `Generate Story` once the visible story/status result renders. Verification passed with `python -m compileall submission tests/submission tests/e2e`, `pytest -q tests/submission/test_app.py tests/submission/test_controller.py tests/submission/test_presentation.py` (`35 passed`), and `pytest -q tests/e2e/test_browser_submission_flow.py` (`3 passed`).
- remaining issue: The elapsed timer is intentionally UI-facing rather than precise job telemetry, so it reports wall-clock seconds until the response completes but does not expose stage-level pipeline progress.
- next best step: If users want more transparency later, add stage-aware status messages such as `Analyzing images...`, `Planning narrative...`, and `Writing story...` on top of the current reliable elapsed-time banner.

### 2026-04-09
- changed files: `submission/styles.py`, `tests/submission/test_app.py`, `tests/e2e/test_browser_submission_flow.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Completed the remaining submission-layer responsive `UI_STATE_ERROR`. The actual issue was not a hidden placeholder column, but an overly narrow stack breakpoint: the workspace only switched out of the 5/7 desktop split at `1180px`, so medium-width browser windows around `1200px` still kept the two-column flex layout and produced a large blank area under the shorter left column while the taller results column stayed pinned on the right. The responsive breakpoint now promotes stacking earlier so both columns become full-width before that medium-width dead zone.
- evidence: Browser measurement at `1200px` before the patch showed `stacked=false` with `.workspace-input-column` at `470px` wide and `.workspace-result-column` at `658px` wide on the same row. After the patch, the browser regression now runs at `1200px` and asserts same-x, stacked-y ordering, and equal widths for the two workspace columns.
- remaining issue: Extremely wide but short screens still use the desktop sticky layout by design, so future tuning may still want a height-aware breakpoint if usability reports come from ultrawide laptop displays.
- next best step: If more layout issues show up, add one more browser regression around the tabs/action-row wrap state after generation so medium-width overflow is protected too.

### 2026-04-09
- changed files: `submission/styles.py`, `tests/submission/test_app.py`, `tests/e2e/test_browser_submission_flow.py`, `pyproject.toml`, `projects/visual-storytelling/TASKS.md`
- what improved: Fixed the real browser-side `UI_STATE_ERROR` behind `Generate Story`. The actual blocker was the custom `APP_HEAD` runtime injected into the Gradio page: its document-wide observer and fetch/busy-state hooks could stall Gradio hydration and prevent the queue-completion UI update from reconciling in the browser. That injected head script has been removed so the native Gradio event/render cycle can complete normally. Browser regression coverage was added for `upload -> click Generate Story -> visible story/status`, plus a narrow-viewport stacking check for the main workspace columns.
- evidence: Ran a side-by-side browser reproduction using stubbed Gradio apps on two local ports. With the injected head script enabled, the browser page stopped hydrating cleanly and the real rendered controls were not available reliably. With the same UI and backend bindings but no injected head runtime, a Selenium-driven Chrome run successfully uploaded an image, clicked `Generate Story`, and rendered `Browser Verified Story` with a visible `Story ready` banner. Added equivalent automated coverage in `tests/e2e/test_browser_submission_flow.py`.
- remaining issue: The browser regression uses a stubbed pipeline so the frontend path is deterministic. That protects the real click/render chain, but it does not replace separate provider-backed qualitative checks for story quality.
- next best step: Keep one lightweight manual smoke pass against the live provider-backed app when changing Gradio or submission-layer JS/CSS, while leaving the deterministic browser e2e test as the default regression guard.

### 2026-04-09
- changed files: `submission/app.py`, `submission/controller.py`, `submission/styles.py`, `tests/submission/test_app.py`, `tests/submission/test_controller.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Fixed two concrete submission-layer `UI_STATE_ERROR`s. The upload control now resets and synchronizes the visible workspace state immediately, generation actions now use Gradio's queued event path instead of the brittle direct predict path, and controller-side validation/runtime failures are rendered into the persistent status/story/diagnostics panels instead of relying on transient UI behavior. The responsive shell was also corrected by removing the compounded Gradio wrapper squeeze on narrow screens, then letting tabs and action rows wrap cleanly at tablet/mobile widths.
- evidence: Re-ran syntax/import sanity with `python -m compileall submission tests/submission`, then submission-focused verification with `pytest -q tests/submission/test_app.py tests/submission/test_controller.py tests/submission/test_presentation.py` and got `33 passed`. Browser inspection before the responsive patch showed the 360px layout collapsing the main workspace row to only `248px` because the custom container width and Gradio's own `.main` padding were compounding; the new CSS removes that extra shell padding pressure and adds smaller-screen wrap guards.
- remaining issue: The Gradio browser event path is more stable now, but it is still a custom-script-assisted wrapper over Gradio internals, so a future Gradio frontend change could still affect the exact busy-state timing or queued-generation UX.
- next best step: Add one focused Playwright regression for `upload -> generate -> status/story text changes` so the browser-side generate path is protected at the same level as the controller and presentation tests.

### 2026-04-09
- changed files: `packages/services/evaluation_service.py`, `tests/services/test_generation_modes.py`, `logs/2026-04-09_evaluation-reconciliation/03-change-log.md`, `projects/visual-storytelling/TASKS.md`
- what improved: Fixed a real evaluation-layer mismatch where provider responses could return score-related flags that did not match the numeric scores, which in turn caused unnecessary revision loops and misleading `revision_limit_reached` states. The evaluator now normalizes those flags against the actual thresholds and can reconcile an under-scored `sentiment_fit_score` upward when a strong deterministic sentiment audit shows the tone cues are clearly present.
- evidence: Added regression coverage for both stale score-flag cleanup and sentiment-fit reconciliation, then ran `pytest -q tests/services/test_generation_modes.py tests/contracts/test_sentiment_audit.py tests/submission/test_presentation.py` with `33 passed` and the full suite with `pytest -q` with `83 passed`. Fresh provider-backed runtime traces now show `happy`, `sad`, `playful`, and a verified `mysterious` run completing with `revision_attempts=0` and no final flags on the park storyboard.
- remaining issue: `suspenseful` is still genuinely weak on gentle scenes because both the provider evaluator and the deterministic audit keep it below threshold, so the remaining problem is now much more localized and honest.
- next best step: Treat `suspenseful` as the last targeted quality track and either add low-intensity suspense exemplars or make the storyboard-aware sentiment target explicitly lighter when the observations do not support stronger tension.

### 2026-04-09
- changed files: `packages/prompts/story_generation_prompts.py`, `packages/core/sentiment_audit.py`, `tests/contracts/test_sentiment_audit.py`, `tests/services/test_generation_modes.py`, `logs/2026-04-09_multi-sentiment-tuning/03-change-log.md`, `projects/visual-storytelling/TASKS.md`
- what improved: Expanded the multi-sentiment tuning pass beyond `suspenseful` by adding provider-facing guidance for `sad`, `mysterious`, and `playful`, plus deterministic audit aliases for common provider wording in those tones. This made `sad` and `playful` materially stronger on the same 3-image storyboard and reduced one major audit mismatch for `mysterious`.
- evidence: Re-ran focused regressions with `pytest -q tests/contracts/test_sentiment_audit.py tests/services/test_generation_modes.py tests/submission/test_presentation.py` and got `31 passed`, then ran the full suite with `pytest -q` and got `81 passed`. Real provider-backed runtime traces in `logs/runtime/2026-04-09_sentiment_runs.jsonl` now show `sad` at `0.85 / 0.99` and `playful` at `0.85 / 0.82` for `sentiment_fit / audit_score` on the tested storyboard.
- remaining issue: `mysterious` is now read more fairly by the deterministic audit, but provider-side sentiment scoring still fluctuates on gentle park scenes, similar to the remaining `suspenseful` weakness.
- next best step: either make the evaluator sentiment expectations scene-aware for low-conflict storyboards, or introduce sentiment-specific reference examples so `mysterious` and `suspenseful` revisions do not keep drifting near the threshold.

### 2026-04-09
- changed files: `packages/prompts/story_generation_prompts.py`, `packages/services/story_generation_service.py`, `tests/services/test_generation_modes.py`, `logs/2026-04-09_suspenseful-prompt-tuning/03-change-log.md`, `projects/visual-storytelling/TASKS.md`
- what improved: Tightened the provider-facing `suspenseful` generation instructions so the model is asked to use explicit but grounded tension cues, avoid off-screen threats, and keep revision attempts anchored to the raw observations. This reduces one major failure mode where suspense drifted into invented menace during retries.
- evidence: Added prompt-capture coverage for the new suspense guidance and for re-including observations during revision, then ran `pytest -q tests/services/test_generation_modes.py` with `8 passed` and the full suite with `pytest -q` with `77 passed`. Re-ran the real provider path on the same 3-image park storyboard and recorded mixed but improved suspense traces in `logs/runtime/2026-04-09_sentiment_runs.jsonl`, including `2026-04-09T06:32:54Z` at `sentiment_fit=0.65 / audit_score=0.71`.
- remaining issue: `suspenseful` is still not reliable enough on the tested gentle park storyboard. The provider now invents less overt threat language in some runs, but it still does not consistently clear the sentiment threshold and can slide back into vague unease.
- next best step: Teach the evaluator and generator a shared definition of low-intensity suspense for ordinary scenes, or downgrade the expected suspense intensity when the observations do not support stronger tension cues.

### 2026-04-09
- changed files: `submission/presentation.py`, `packages/infra/sentiment_run_logger.py`, `tests/submission/test_presentation.py`, `logs/2026-04-09_provider-ui-verification/03-change-log.md`, `projects/visual-storytelling/TASKS.md`
- what improved: Clarified the trace semantics around revision exhaustion versus provider fallback. The UI no longer tells the user "Fallback used" when the real condition is that the draft hit the revision limit with remaining evaluator flags, and the runtime JSONL log now adds an explicit `revision_limit_reached` field so provider-backed runs are easier to read correctly.
- evidence: The earlier provider verification logs showed `provider_status=provider` alongside `is_fallback=true`, which was technically consistent with the current contract but easy to misread. After the wording change, the diagnostics surface now labels that state as `Revision limit reached`, while real local-provider outages remain labeled separately as `Local fallback active`. Re-ran `pytest -q tests/submission/test_presentation.py` to confirm the updated copy path.
- remaining issue: The internal field name `is_fallback` still carries historical meaning in the domain model and session snapshot, so the code is clearer at the UI/log boundary but not yet fully renamed across the contract surface.
- next best step: If we decide this trace vocabulary should be permanent, promote `revision_limit_reached` into the result contract itself in a dedicated contract update rather than overloading `is_fallback`.

### 2026-04-09
- changed files: `packages/core/sentiment_audit.py`, `tests/contracts/test_sentiment_audit.py`, `logs/2026-04-09_provider-ui-verification/00-status-board.md`, `logs/2026-04-09_provider-ui-verification/03-change-log.md`, `projects/visual-storytelling/TASKS.md`
- what improved: Ran a real provider-backed verification pass and then revised the deterministic sentiment-audit aliases so provider-written `happy` and `heartwarming` drafts no longer get under-credited for using natural synonyms such as `joy`, `happiness`, `bond`, and `calm`. The verification trail now clearly distinguishes sentiments that are working well on the tested storyboard from those that still need prompt work, especially `suspenseful`.
- evidence: Verified the provider path with recent runtime log entries that record only `provider` execution modes, including `happy` at `sentiment_fit=0.75 / audit_score=0.91`, `heartwarming` at `0.75 / 0.99`, and `suspenseful` at `0.65 / 0.59` on the same 3-image storyboard. Re-ran the full suite with `pytest -q` and got `75 passed`. Browser smoke at 760px also confirmed stacked layout plus a real `POST /gradio_api/run/predict` from `Generate Story`.
- remaining issue: The provider model still struggles to keep `suspenseful` both grounded and strongly expressed on a gentle park storyboard; this now looks like a real generation/evaluation weakness rather than just an audit false positive.
- next best step: Tighten the `suspenseful` generation prompt so it prefers restrained tension over invented threat escalation, then re-run the same storyboard verification log to see whether `sentiment_fit` can move above threshold without hurting grounding.

### 2026-04-09
- changed files: `packages/core/models/sentiment_audit.py`, `packages/core/models/evaluation.py`, `packages/core/sentiment_audit.py`, `packages/core/models/__init__.py`, `packages/prompts/story_generation_prompts.py`, `packages/services/story_generation_service.py`, `packages/services/evaluation_service.py`, `packages/services/pipeline.py`, `packages/infra/bootstrap.py`, `packages/infra/sentiment_run_logger.py`, `submission/presentation.py`, `docs/02_architecture/data-contracts.md`, `logs/README.md`, `tests/contracts/test_story_contracts.py`, `tests/services/test_generation_modes.py`, `tests/submission/test_presentation.py`, `tests/smoke/test_local_fallback_pipeline.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Added an explicit sentiment-audit contract and runtime sentiment trace logging so every run now records the selected sentiment, resolved profile, generated story, evaluator scores, and deterministic tone-cue evidence in `logs/runtime/*_sentiment_runs.jsonl`. The local fallback generation path was also strengthened so each supported sentiment now produces visibly different grounded phrasing instead of swapping only one adjective, and the fallback evaluator now scores sentiment fit from the actual detected cues rather than returning a near-constant placeholder score.
- evidence: First ran a focused probe across all six sentiments and observed that the old fallback outputs were nearly identical apart from one tone word and always reported `sentiment_fit=0.86`. After the changes, a fresh probe showed distinct phrasing per sentiment and audit-backed scores such as `happy=0.99`, `sad=0.89`, `suspenseful=0.91`, `mysterious=0.80`, `heartwarming=0.89`, and `playful=0.89`, each with a matching audit summary. Also ran `pytest -q` and the suite passed.
- remaining issue: The deterministic audit is intentionally explainable, so it still favors explicit tone cues over subtle literary style; a provider-written draft could occasionally feel correct to a human while the heuristic audit asks for stronger wording.
- next best step: Add one small browser-level regression that checks the Diagnostics tab shows the new sentiment audit after generation, then gather a few real provider-backed runs to compare heuristic audit summaries against human judgement.

### 2026-04-09
- changed files: `submission/app.py`, `submission/styles.py`, `tests/submission/test_app.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Reduced the current submission-layer `UI_STATE_ERROR` around narrow screens and result visibility. The workspace now collapses to a full-width stacked layout earlier, before the two panels become cramped, and every generation action now scrolls the user directly to the refreshed result area so the story/status panel is visible immediately after clicking `Generate Story`.
- evidence: Re-ran `pytest -q tests/submission/test_app.py tests/submission/test_controller.py tests/submission/test_presentation.py` with `32 passed`. Also inspected the built Gradio config and confirmed all five generation buttons now set `scroll_to_output=True`, and the CSS now includes the `max-width: 1180px` wrap guard that forces both workspace columns to 100% width when the viewport narrows.
- remaining issue: This closes the main layout/visibility gap without reintroducing the older upload-change callback, but it does not yet add a true browser-level smoke test for `upload -> generate -> scrolled story visible`, so a future Gradio rendering change could still affect the exact front-end behavior.
- next best step: Add one focused browser smoke test that verifies the result panel is reachable and visible on a tablet-width viewport after clicking `Generate Story`, so this fix is protected beyond config-level assertions.

### 2026-04-09
- changed files: `submission/styles.py`, `tests/submission/test_app.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Fixed the remaining `UI_STATE_ERROR` on the main generation action. The custom busy-state helper in `APP_HEAD` was marking the page busy during the document capture phase, which disabled `Generate Story` before Gradio could dispatch the actual click handler. The busy indicator is now deferred to the next tick, so the request is still sent while the UI keeps its loading feedback.
- evidence: Added a regression assertion for the deferred busy-state script, re-ran `pytest -q tests/submission/test_app.py tests/submission/test_controller.py` with `16 passed`, and re-ran the full suite with `pytest -q` with `68 passed`. Also revalidated the exact browser path with Playwright against the live local app and confirmed that after upload, clicking `Generate Story` now issues `POST /gradio_api/run/predict` instead of stalling at the button.
- remaining issue: The busy-state helper is working again, but it still depends on custom client-side script around Gradio internals, so future Gradio event-path changes could regress this area if the helper becomes more aggressive again.
- next best step: Add one focused browser smoke test for `upload -> click Generate Story -> request dispatched -> result area updates` so this exact front-end regression is caught automatically rather than only through manual reports.

### 2026-04-09
- changed files: `submission/app.py`, `submission/controller.py`, `tests/submission/test_app.py`, `tests/submission/test_controller.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Fixed the upload-triggered `UI_STATE_ERROR` that left the sentiment controls and generate path effectively unusable after selecting an image. The file uploader no longer binds a live upload/reset callback, uploader-side reordering remains disabled, and the app now favors a stable `upload -> choose sentiment -> set max sentences -> generate` flow over the earlier eager preview-reset behavior that was freezing the front end. The upload reset helper still exists for controller-level reuse, but the live binding that was interfering with post-upload interaction has been removed.
- evidence: Re-ran submission-focused tests with `pytest -q tests/submission/test_app.py tests/submission/test_controller.py` and got `15 passed`, then re-ran the full suite with `pytest -q` and got `67 passed`. Also validated the exact failure path with Playwright against the running local app: after setting a file on the uploader, clicking the second sentiment chip successfully changed the checked value from `happy` to `sad`.
- remaining issue: The app is now interactable again after upload, but the sequence preview and workspace reset no longer update immediately on file selection; they update only once the user actually generates a story. That is a deliberate tradeoff to restore reliable interaction first.
- next best step: Reintroduce immediate upload preview in a safer way, ideally through a lightweight client-only preview shell or a separate explicit `Refresh preview` action, so we can keep the working post-upload controls without returning to the Gradio event path that froze the page.

### 2026-04-09
- changed files: `packages/core/models/story.py`, `packages/core/models/__init__.py`, `packages/infra/provider_client.py`, `packages/services/scene_analysis_service.py`, `packages/services/narrative_planning_service.py`, `packages/services/story_generation_service.py`, `packages/services/evaluation_service.py`, `packages/services/pipeline.py`, `submission/presentation.py`, `docs/02_architecture/data-contracts.md`, `tests/contracts/test_story_contracts.py`, `tests/services/test_pipeline_trace_fields.py`, `tests/submission/test_presentation.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Replaced the ad-hoc provider-status inference with a typed `provider_status` contract on `StoryResult`. Provider-backed services now record whether they ran through the real provider or a local fallback, along with a concrete reason and recovery hint, and the pipeline preserves that trace for the UI. Diagnostics now reads this typed trace directly instead of guessing from `grounding_notes` and evaluator summary text, while still keeping the older notes as compatibility backup.
- evidence: Added contract coverage for `provider_status`, pipeline trace coverage that verifies stage ordering and fallback forwarding, and presentation coverage that checks the Diagnostics tab renders the new provider-status trail. Also ran one real local generation and confirmed the result now carries typed statuses for `scene_analysis`, `narrative_planning`, `story_generation`, and `evaluation`. Re-ran the full suite with `pytest -q` and got `66 passed`.
- remaining issue: The typed trace now captures stage, mode, reason, and recovery hint, but scene analysis is still aggregated at the stage level rather than reporting one status per image when a mixed sequence partially falls back.
- next best step: If partial per-image provider behavior becomes important, extend `provider_status` with an optional image scope or detail field so the UI can distinguish `all images fell back` from `only one frame needed local fallback`.

### 2026-04-09
- changed files: `packages/infra/provider_client.py`, `packages/services/scene_analysis_service.py`, `packages/services/narrative_planning_service.py`, `packages/services/story_generation_service.py`, `packages/services/evaluation_service.py`, `submission/presentation.py`, `tests/services/test_provider_failure_fallbacks.py`, `tests/submission/test_presentation.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Tightened the fallback path behind the submission app so provider outages now degrade into each service's deterministic local fallback instead of leaking generic mock artifacts into the visible story flow. The diagnostics panel also now exposes a dedicated provider-status trail and downgrades fully local runs from `Story ready` to a more honest draft state, making it clear whether the app used a real provider, lacked `OPENAI_API_KEY`, lacked the SDK, or hit a provider/network failure. While doing this, the local filename heuristic was also cleaned up so image subjects no longer accidentally absorb file extensions like `png`.
- evidence: Added service tests covering provider-request failure fallback for scene analysis, story generation, and evaluation, plus presentation coverage for the new provider-status diagnostics. Ran a real local generation check and confirmed the Diagnostics HTML contains `Provider status`, `Local fallback active`, and a concrete cause string. Re-ran the full suite with `pytest -q` and got `66 passed`.
- remaining issue: The provider reason is now visible and trustworthy, but it is still inferred in the UI from notes and summaries rather than exposed as a first-class typed trace field, so future diagnostics work may want a dedicated contract if this area keeps growing.
- next best step: Add a small typed `provider_status` payload to `StoryResult` and render it directly in Diagnostics so fallback cause, stage, and recovery hint no longer depend on text parsing.

### 2026-04-09
- changed files: `packages/infra/provider_client.py`, `pyproject.toml`, `requirements.txt`, `tests/contracts/test_story_contracts.py`, `tests/smoke/test_provider_integration.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Fixed an `API_INTEGRATION_ERROR` at the packaging boundary. A clean editable install could not import `packages.infra.provider_client` because `instructor` and `openai` were used directly but not declared as install requirements. The provider client now degrades cleanly to local fallback behavior when those SDKs are unavailable, and the package metadata now explicitly installs both dependencies so fresh environments can run the app and still use the real provider path when configured.
- evidence: Reproduced the failure in a new virtualenv with `pip install -e .`, which previously raised `ModuleNotFoundError: No module named 'instructor'` while importing `packages.infra.provider_client`. Added a packaging contract test for dependency declaration and a smoke test proving `ProviderClient` falls back safely when the SDK layer is absent. Verification after the change will re-run the focused tests plus the clean-venv import check.
- remaining issue: The app now survives missing provider SDKs and installs them by default, but local fallback remains intentionally conservative; without a valid API key and working network, image understanding still downgrades to deterministic backup behavior rather than full multimodal analysis.
- next best step: Surface an explicit diagnostics note in the submission UI that distinguishes `missing API key`, `missing provider SDK`, and `network/provider failure` so users can tell whether they are seeing a conservative fallback because of setup or because of a runtime outage.

### 2026-04-09
- changed files: `submission/app.py`, `submission/styles.py`, `submission/presentation.py`, `packages/infra/provider_client.py`, `packages/services/narrative_planning_service.py`, `packages/services/story_generation_service.py`, `packages/services/evaluation_service.py`, `tests/submission/test_app.py`, `tests/submission/test_presentation.py`, `tests/services/test_generation_modes.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Addressed a new `UI_STATE_ERROR` and trust-friction loop in the submission demo. The primary controls were moved higher in the left column so sentiment selection and `Generate Story` stay reachable in fullscreen layouts, action buttons now advertise a visible in-progress state through a non-blocking busy toast, and the run-comparison summary no longer repeats `Limited comparison` or shows misleading highlight deltas when the runs are not directly comparable. On the generation side, provider text temperatures are now stage-specific so planning, story writing, and evaluation behave more conservatively and produce fewer avoidable fallback loops.
- evidence: Added test coverage for the busy-shell injection and for non-comparable run summaries not duplicating labels, plus service coverage for the new stage-specific text temperatures. Re-ran the full suite with `pytest` and got `60 passed`. Also re-checked the fullscreen layout in a browser harness and confirmed the `Sentiment` chips and `Generate Story` button now land inside the initial viewport instead of below the fold.
- remaining issue: The demo still depends on Gradio’s stock file uploader and event layer, so there may still be browser-specific quirks around post-upload interaction timing that are harder to eliminate without moving to a more custom input surface.
- next best step: Add one targeted browser smoke test for `upload -> generate -> regenerate -> tab switch` so we can catch any remaining Safari/Gradio interaction regressions automatically instead of only through manual reports.

### 2026-04-09
- changed files: `submission/app.py`, `submission/styles.py`, `tests/submission/test_app.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Fixed a Safari-facing `UI_STATE_ERROR` where browser dark-mode preference forced Gradio native controls into a dark palette while the custom story surfaces stayed light, making the workspace look broken and mismatched. The submission shell now force-locks the page into light mode through a launch-time head script, adds defensive `body.dark` fallback styling for native components, and hides the default Gradio footer so the fullscreen layout no longer picks up extra visual clutter at the bottom.
- evidence: Added submission-level coverage for the light-mode shell injection and fallback CSS. Re-ran the full suite with `pytest` and got `58 passed`. Also verified in a dark-mode-emulated browser run that the page now reports `bodyClass='light'`, `colorScheme='light'`, and a white Gradio file-upload background instead of the previously broken dark native controls.
- remaining issue: The uploader is still the stock Gradio component, so if we later want pixel-perfect control over upload/reorder affordances across Safari and Chromium we will eventually need a more custom input shell than the native widget provides.
- next best step: Add one small browser smoke harness for `upload -> generate -> tab switch` so Safari/Gradio interaction regressions are caught automatically alongside the existing pytest suite.

### 2026-04-09
- changed files: `submission/app.py`, `submission/presentation.py`, `submission/styles.py`, `tests/submission/test_app.py`, `tests/submission/test_presentation.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Addressed a fresh `UI_STATE_ERROR` in the local workspace flow by removing Gradio progress overlays from the submission interactions, compacting the left-side sequence preview into a horizontal strip instead of a tall grid, and turning the desktop workspace into a stable two-panel layout with sticky panels and internal tab scrolling. The CSS also now explicitly disables pointer interception from Gradio status trackers, which reduces the “results appeared but tabs/buttons stopped responding” failure mode the user hit after generation.
- evidence: Added presentation coverage for the new compact sequence preview markup and app CSS coverage for the new interaction/layout guards. Ran the full suite with `pytest` and got `57 passed`.
- remaining issue: Browser automation still shows some timing variance around file-upload-triggered generation in headless mode, so the UI is more stable but not yet backed by a full end-to-end browser test that proves post-generation tab interaction every time.
- next best step: Add a targeted Playwright regression that drives one real local generation through the Gradio UI and asserts post-generation interaction on `Story / Analysis / Diagnostics` so future layout tweaks cannot silently reintroduce the click-blocking state.

### 2026-04-09
- changed files: `packages/infra/provider_client.py`, `packages/infra/upload_persistence.py`, `packages/services/scene_analysis_service.py`, `packages/services/narrative_planning_service.py`, `packages/services/story_generation_service.py`, `packages/services/evaluation_service.py`, `submission/styles.py`, `tests/smoke/test_local_fallback_pipeline.py`, `tests/submission/test_app.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Fixed the worst local-demo failure path by removing slow network retries when no OpenAI key is configured and replacing the generic mock chain with deterministic local fallbacks for scene analysis, planning, generation, and evaluation. The submission UI also now uses a wider fullscreen container, stronger root background coverage, and explicit `visually-hidden` guards so the page no longer collapses into a narrow left column with stray duplicate tab content. This directly addresses the trust and stability gap behind the broken fullscreen layout and the repeated bad-story path in local use.
- evidence: Added a smoke test proving the pipeline now produces a non-mock grounded story without an API key, plus app CSS coverage for the fullscreen layout guards. Ran `pytest` with result `56 passed`.
- remaining issue: The local fallback is intentionally conservative and much better than the old generic mock, but it is still only a deterministic backup; with no multimodal provider configured it cannot match real image understanding on complex photos.
- next best step: Add a small in-app diagnostics note that explicitly says when local fallback mode is active and link it to setup guidance for a real multimodal provider, so users understand why the story is conservative instead of assuming the model is misbehaving.

### 2026-04-09
- changed files: `packages/core/models/story.py`, `submission/controller.py`, `submission/app.py`, `docs/02_architecture/data-contracts.md`, `tests/contracts/test_story_contracts.py`, `tests/submission/test_controller.py`, `tests/submission/test_app.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Removed the hard 8-sentence ceiling from `StoryRequest`, switched the submission UI from a fixed slider to an open-ended positive integer input, and normalized sentence-count input in the controller so large or malformed values fail safely with a clear UI error instead of leaking a raw validation problem. This also keeps the app thin while letting users request longer stories when they want them.
- evidence: Added contract coverage for large `max_sentences`, controller coverage for normalizing float-like UI input and rejecting non-positive values, and app coverage for the updated sentiment UI wiring. Ran `pytest` with result `54 passed`.
- remaining issue: Users can now request any positive sentence count, but the provider may still choose to stop earlier when the available image evidence or narrative plan is too sparse to support a long grounded story.
- next best step: Add presentation guidance near the sentence-count input and result card that explains the request is a ceiling, not a guarantee, then consider exposing a separate "story length" mode that balances brevity against exhaustive retelling.

### 2026-04-09
- changed files: `submission/styles.py`, `tests/submission/test_app.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Added restrained sentiment icons directly to the input radio chips so the tone selector now matches the rest of the trust-first visual language. The change stays UI-only, keeps the existing sentiment values and thin app boundary intact, and makes the selection step feel more expressive without turning it into emoji-heavy decoration.
- evidence: Added tests that pin the supported sentiment order used by the CSS icon mapping and verify the icon selectors exist in `APP_CSS`. Ran `pytest tests/submission/test_app.py tests/submission/test_presentation.py tests/submission/test_controller.py` with result `23 passed`.
- remaining issue: The sentiment selector is now more expressive, but it still relies on positional CSS mapping, so if the supported sentiment order changes later the icon styling will need to be updated alongside it.
- next best step: Move the sentiment icon metadata into a small presentation-owned mapping so labels, helper copy, and styling can share one explicit source of truth without relying on option position.

### 2026-04-09
- changed files: `submission/presentation.py`, `submission/styles.py`, `tests/submission/test_presentation.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Added trust-safe visual personality to the submission UI by introducing small semantic icons in status banners, metadata chips, and run-comparison headings, plus a compact mascot illustration in the primary empty states. The result remains confidence-first, but the workspace now feels more intentional and approachable without turning warnings or diagnostics into playful noise.
- evidence: Added presentation tests for icon-bearing status markup and empty-state mascot rendering. Ran `pytest tests/submission/test_presentation.py tests/submission/test_controller.py tests/submission/test_app.py` with result `21 passed`.
- remaining issue: The UI now has clearer personality in empty and status states, but the sentiment selector itself is still text-only and does not yet carry lightweight visual cues.
- next best step: Add restrained sentiment-specific icons to the radio choices and the story meta area so tone selection feels more expressive while still preserving trust-first hierarchy.

### 2026-04-09
- changed files: `submission/session_state.py`, `submission/controller.py`, `submission/presentation.py`, `submission/app.py`, `submission/styles.py`, `packages/core/models/story.py`, `packages/services/pipeline.py`, `docs/02_architecture/data-contracts.md`, `tests/contracts/test_story_contracts.py`, `tests/services/test_pipeline_trace_fields.py`, `tests/submission/test_presentation.py`, `tests/submission/test_controller.py`, `tests/submission/test_app.py`, `tests/smoke/test_storytelling_pipeline_smoke.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Added a lightweight session-scoped previous-run vs current-run comparison layer without introducing persistent history. The submission flow now retains only the latest successful run snapshot, compares it against the immediately previous run, and surfaces deterministic trust signals in two places: a compact verdict block near the story result and a detailed diagnostics comparison table. The comparison also tracks whether the two runs are directly comparable by checking images and sentiment, and it preserves metadata for correction count and strict-grounding mode so users can see whether treatment changes likely explain quality shifts.
- evidence: Added tests for score delta rendering, fallback and warning-count changes, limited-vs-direct comparison labeling, deterministic verdict generation, no-previous-run handling, and snapshot preservation on failed runs. Ran `pytest` with result `48 passed`.
- remaining issue: The session comparison now shows whether the latest run improved or not, but it still compares only one previous run and does not yet highlight which specific correction most likely drove a score change.
- next best step: Add a compact causal hint layer that links changed analysis fields to the biggest score or flag movements between the previous and current run, while still keeping the experience deterministic and lightweight.

### 2026-04-08
- changed files: `packages/core/models/story.py`, `packages/services/pipeline.py`, `submission/app.py`, `submission/controller.py`, `submission/presentation.py`, `submission/styles.py`, `docs/02_architecture/data-contracts.md`, `tests/contracts/test_story_contracts.py`, `tests/services/test_pipeline_trace_fields.py`, `tests/submission/test_presentation.py`, `tests/submission/test_controller.py`, `tests/submission/test_app.py`, `tests/smoke/test_storytelling_pipeline_smoke.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Closed the visual trust loop for corrections by preserving both original and effective observations in `StoryResult`, then rendering compact raw-vs-corrected comparison blocks directly in the Analysis editor cards. Users can now inspect changed versus unchanged fields before regeneration, and a new combined action runs corrected analysis together with strict grounding in one step. Story metadata also now surfaces deterministic indicators for corrected analysis, strict grounding, and correction counts directly near the result card.
- evidence: Added comparison rendering coverage, combined-action controller coverage, preview-reset coverage, and contract checks for original observations. Ran the full suite with `pytest` and got `42 passed`.
- remaining issue: Corrected previews are now visible and actionable, but the UI still does not show a dedicated before/after summary at the sequence level or explain how strict grounding altered evaluator outcomes compared with the default run.
- next best step: Add a compact sequence-level diff summary with evaluator delta cues so users can see whether corrected analysis or strict grounding materially improved grounding/coherence across runs.

### 2026-04-08
- changed files: `packages/core/models/scene.py`, `packages/core/models/correction.py`, `packages/core/models/story.py`, `packages/core/models/__init__.py`, `packages/services/observation_override_service.py`, `packages/services/pipeline.py`, `packages/services/sequence_linking_service.py`, `packages/services/narrative_planning_service.py`, `packages/services/story_generation_service.py`, `packages/services/evaluation_service.py`, `packages/services/sentiment_control_service.py`, `packages/infra/bootstrap.py`, `packages/prompts/evaluation_prompts.py`, `submission/app.py`, `submission/controller.py`, `submission/presentation.py`, `submission/styles.py`, `docs/02_architecture/data-contracts.md`, `tests/contracts/test_story_contracts.py`, `tests/services/test_observation_override_service.py`, `tests/services/test_generation_modes.py`, `tests/services/test_pipeline_trace_fields.py`, `tests/services/test_pipeline_refinement.py`, `tests/submission/test_presentation.py`, `tests/submission/test_controller.py`, `tests/submission/test_app.py`, `tests/smoke/test_storytelling_pipeline_smoke.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Added a bounded user-correction layer for scene analysis and routed it through the real pipeline as `scene observations -> user overrides -> effective observations -> sequence linking`. The Analysis tab now exposes structured per-image correction cards for main subject, setting, visible action, continuity with the previous image, and an optional generation note. The UI also now supports distinct `Regenerate`, `Regenerate with stricter grounding`, and `Generate from corrected analysis` paths, while keeping `submission/app.py` as a thin Gradio wiring layer by moving callback logic into `submission/controller.py`.
- evidence: Added override-flow tests, strict-grounding policy tests, controller tests for active corrections, and presentation tests for corrected-analysis and strict-mode transparency. Ran `pytest` across the full suite with result `38 passed`.
- remaining issue: The editor currently supports bounded correction and regeneration, but it does not yet expose side-by-side raw-vs-corrected analysis diffs or sentiment-only regeneration on top of corrected analysis.
- next best step: Add an explicit comparison view showing original analysis versus corrected effective observations, then support combined `corrected analysis + stricter grounding` regeneration without increasing UI ambiguity.

### 2026-04-08
- changed files: `packages/core/models/story.py`, `packages/services/pipeline.py`, `submission/app.py`, `submission/presentation.py`, `submission/styles.py`, `docs/02_architecture/data-contracts.md`, `tests/contracts/test_story_contracts.py`, `tests/services/test_pipeline_trace_fields.py`, `tests/submission/test_presentation.py`, `tests/submission/test_app.py`, `tests/smoke/test_storytelling_pipeline_smoke.py`, `projects/visual-storytelling/TASKS.md`
- what improved: Addressed the `UI_STATE_ERROR` trust gap by shifting the demo to a confidence-first result presentation. Low-confidence and fallback outputs now receive an explicit warning banner ahead of the prose, the main workspace is split into Story / Analysis / Diagnostics tabs, ordered thumbnail previews are visible before generation, and sentence-to-image grounding trace is preserved into `StoryResult` for a Story Map UI.
- evidence: Added contract coverage for `sentence_alignment` and `grounding_notes`, a pipeline forwarding test for trace preservation, presentation tests for `good` / `caution` / `low_confidence` states, and submission-level smoke coverage for UI construction and ordered thumbnail preview rendering.
- remaining issue: Manual analysis correction, stricter-grounding toggles, and alternate regenerate actions are still deferred, so users can inspect uncertainty but cannot yet edit the analysis in-place before regeneration.
- next step: Implement the analysis-correction workflow and stricter-grounding controls so uncertain sequences can be repaired before story generation.

### 2026-04-08
- changed files: `projects/visual-storytelling/PROJECT.md`, `projects/visual-storytelling/SKILLS/image-analysis.SKILL.md`, `projects/visual-storytelling/SKILLS/story-generation.SKILL.md`, `projects/visual-storytelling/SKILLS/sentiment-control.SKILL.md`, `projects/visual-storytelling/SKILLS/ui-flow.SKILL.md`, `DESIGN.md`, `projects/visual-storytelling/TASKS.md`
- what improved: the high-priority project documents now define the MVP purpose, stage boundaries, failure taxonomies, UI flow, and visual design in one consistent vocabulary; `DESIGN.md` now gives a project-specific storytelling UI system that matches the architecture and debug-visibility rules
- evidence: reviewed the updated documents after editing and confirmed the new sections cover ordered-image storytelling, typed stage outputs, optional debug inspection, trace-first trust cues, and story-first result hierarchy
- remaining issue: the documents are now aligned, but the actual implementation still needs to be checked against them, especially the thin `submission/app.py` boundary and the UI presentation of intermediate outputs
- next step: inspect the current app and pipeline against the updated docs, then make the smallest changes needed to match the documented MVP flow

### 2026-04-08
- changed files: `submission/app.py`, `packages/core/models/*`, `packages/services/*`, `tests/*` (Inspected, no changes needed as they already comply)
- what improved: Verified that the actual implementation complies with the updated document stack. The codebase already features a thin `/submission/app.py`, proper typed contracts in `packages/core/models`, clean service boundaries, and separation of prompts. Smoke and contract tests are also present and functional.
- evidence: ran inspection using file viewers on `/submission/app.py`, service files, and tests. Verified that `test_story_contracts.py` and `test_storytelling_pipeline_smoke.py` exist and test the pipeline end-to-end.
- remaining issue: Implementation is solid, but we must ensure future capability additions (like detailed sequence linking logic or evaluation reporting) do not regress this clean architecture.
### 2026-04-08
- changed files: `packages/infra/provider_client.py` (new), `packages/services/scene_analysis_service.py`, `packages/services/story_generation_service.py`, `packages/prompts/story_generation_prompts.py`, `packages/infra/bootstrap.py`, `tests/smoke/test_provider_integration.py` (new), `tests/smoke/test_storytelling_pipeline_smoke.py`
- what improved: Replaced MVP heuristic strings for scene analysis and story generation with an actual LLM provider boundary using Instructor/OpenAI. The logic remains purely structurally sound (typed inputs/outputs) and cleanly isolates the API connection logic into `packages/infra`. Added graceful fallback logic when missing API keys or in mock mode.
- evidence: Created `test_provider_integration.py` which triggers `ProviderClient` in both `use_mock=True` and fallback scenarios. All 7 tests pass locally with `pytest`.
- remaining issue: Output quality was unmeasured and subjective.
- next step: Build a lightweight evaluation pack and regression harness to ensure outputs stay grounded and coherent.

### 2026-04-08 (Phase 2)
- changed files: `packages/services/sequence_linking_service.py`, `packages/services/narrative_planning_service.py`, `packages/services/evaluation_service.py`, `packages/infra/provider_client.py`, `packages/infra/bootstrap.py`, `tests/evals/fixtures.py` (new), `tests/evals/test_eval_pack.py` (new)
- what improved: Resolved the remaining issue by completely migrating the remaining 3 pipeline services (`Sequence Linking`, `Narrative Planning`, and `Evaluation`) to the actual LLM via `ProviderClient`, abandoning the basic heuristics. Then, introduced a new transparent regression/evaluation harness (`tests/evals/`) which structurally evaluates 6 edge cases ranging from grounding risks to sentiment sensitivity tests.
- evidence: Overrode the integration pipeline to inject structural test fixtures directly. Successfully ran `pytest tests/evals/test_eval_pack.py` confirming the 6 eval iterations run and generate metric scores seamlessly.
- remaining issue: We now have real model connections and evaluation harnesses, but actual narrative tuning (prompt engineering for the newly migrated stages) needs cycles to optimize real LLM responses.
- next step: Provide a real API key and run the `test_eval_pack.py` over 5-10 real iterations to establish an initial LLM-based generative baseline metric.

### 2026-04-08 (Phase 3)
- changed files: `packages/services/sequence_linking_service.py`, `tests/services/test_sequence_linking.py` (new)
- what improved: Replaced LLM prompt string match in `SequenceLinkingService` with an extremely structured, deterministic heuristic class. It uses Word overlap to match recurring entities (e.g., 'brown dog' matches 'dog' but 'man' doesn't match 'woman'), propagates static settings instead of dropping them, explicitly builds a transparent event chain, and catches ambiguity risks (no stable subject or erratic settings).
- evidence: Wrote 4 test cases covering identical subject validation, setting progression matching, distinct entity segregation, and ambiguity flagging. Run via `pytest tests/services/test_sequence_linking.py`. Result: 4 passing.
- remaining issue: Narrative planning still operates directly via the LLM API using prompt structures. Real test measurements need to identify if Narrative Planning handles the new deterministic Sequence Memory optimally.
- next step: Tune the narrative planning prompt instructions to take maximum advantage of the new, highly reliable SequenceMemory object.

### 2026-04-08 (Phase 4)
- changed files: `packages/prompts/narrative_planning_prompts.py`, `packages/services/narrative_planning_service.py`, `tests/evals/test_narrative_planning_eval.py` (new)
- what improved: Switched the prompt behavior of `NarrativePlanningService` from "free-form generation" to "constrained planning". The prompt now designates `SequenceMemory` as authoritative law, forcing chronological order, and actively instructs the LLM to write restrictive `forbidden_claims` and conservative `allowed_inferences` on its own when given ambiguous sequences instead of fabricating ungrounded linking drama.
- evidence: Created the LLM eval test `test_narrative_planning_eval.py` which passes a disconnected ambiguous sequence into the real API endpoint. Verified that the output successfully flags ungrounded interactions (e.g. banning connections between the 'stranger' and 'solitary man') and limits `allowed_inferences` to soft bridging language ("time passes"). Test passing: 1/1.
- remaining weakness: The `StoryGenerationService` is still potentially liable to bypass the generated plan and write hallucinatory prose if the underlying foundation model hallucinates. The interaction between Evaluator and Generation Service still isn't fully hardened into an automated rejection loop.
- next step: Implement an iterative Refinement/Rejection mechanism that automatically re-prompts the Generative model if the EvaluationService scores it below threshold, using the Planner's `forbidden_claims` as strict rejection criteria.

### 2026-04-08 (Phase 5)
- changed files: `packages/services/story_generation_service.py`, `packages/services/pipeline.py`, `tests/services/test_pipeline_refinement.py` (new)
- what improved: Implemented a bounded Evaluator-driven Rejection/Refinement Loop. Before returning a `StoryResult`, the pipeline now checks `EvaluationReport.flags`. If any rule breaches are flagged (grounding error, sequence flow violation, etc.), it does NOT rebuild the whole plan but purely asks the Generator to `revise()` the broken draft exactly against the flagged violations and the original authoritative `SequenceMemory`/`NarrativePlan`. Limits attempts to 2 to prevent infinite cycles.
- evidence: Created the unit test file `test_pipeline_refinement.py` using Mock services. The tests verify two scenarios: successfully halting after one corrective iteration, and gracefully falling back (while preserving explicit failure flags for user insight) if the Generator refuses to cooperate after max limits. Passing 2/2 tests.
- remaining weakness: The entire visual storytelling core capability is now heavily structured and evaluated, but we have not verified whether the actual end-user user interface (Gradio app) displays these new evaluation metrics and flags clearly instead of hiding them.
- next step: Harden the UI in `submission/app.py` to elegantly render the evaluation scores and flags or warnings so the user's trust and inspectability constraints (from `AGENTS.md`) are satisfied.

### 2026-04-08 (Phase 6)
- changed files: `packages/core/models/story.py`, `packages/services/pipeline.py`, `submission/presentation.py`
- what improved: Upgraded UI-layer inspectability without muddying the primary story display. Extended the `StoryResult` model to explicitly carry pipeline operation statistics (`revision_attempts`, `is_fallback`). Modified `submission/presentation.py` to dynamically format the `Evaluation Report` Accordion with a "Pipeline Refinement Status" header, visually highlighting success (✅) or fallback warnings (⚠️) alongside clear evaluation constraint scores and flags.
- evidence: UI now gracefully scales to display `StoryResult`, completely decoupled from specific presentation logic according to clean boundary contracts. Markdown generation is isolated in `presentation.py` ensuring thin `app.py` UI logic.
- remaining weakness: We have completed architectural, provider, pipeline, and UI boundaries. However, `submission/app.py` still lacks comprehensive system-level e2e integration testing, which means structural bugs between modules might only be caught iteratively.
- next step: Implement an automated integration test verifying `submission/app.py` Gradio input execution mapping or fully seal the system for final submission.
